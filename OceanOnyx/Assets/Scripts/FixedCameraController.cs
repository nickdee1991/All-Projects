using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FixedCameraController : MonoBehaviour
{
    public Camera[] fixedCamera;

    void Start()
    {
       //define all the cameras in the scene 
    }

    //trigger 1 - camera detects default state and assigns active camera relative to trigger player is inside
    //trigger 2 - camera 1 disables and camera 2 enables once triggers have changed
    //trigger 3
    //trigger 4
}
